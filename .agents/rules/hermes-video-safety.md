# Hermes video safety

- Inspect an existing job before reading broad source files.
- Treat structured `failureCodes`, `nextActions`, hashes, and profile IDs as authoritative.
- Require explicit approval before provider cost, account authentication, upload, or live CapCut acceptance.
- Never store or echo secrets, cookies, OAuth tokens, or API keys.
- Never claim live/GUI acceptance from contract or offline checks.
